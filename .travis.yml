dist: bionic
git:
  depth: false
language: go
go:
  - 1.18.x
env:
  global:
  - DKR_USERNAME=vulcansec
  - CGO_ENABLED=0
  - GO111MODULE=on
go_import_path: github.com/adevinta/vulcan-checks
deploy:
  # - provider: script
  #   script: bash release.sh
  #   on:
  #     all_branches: true
  #     condition: $TRAVIS_BRANCH != "master"
  - provider: script
      # bash release-edge.sh &&
    script: >-
      mkdir pages/checktypes &&
      go install "github.com/adevinta/vulcan-check-catalog/cmd/vulcan-check-catalog@${VCC_VERSION:-main}" &&
      vulcan-check-catalog -registry-url "$DKR_USERNAME" -tag edge -output pages/checktypes/edge.json cmd/
    on:
      branch: checks-catalog
  - provider: pages
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    keep_history: true
    local_dir: ./pages
    on:
      branch: checks-catalog
